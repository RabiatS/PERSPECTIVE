# spatial-viz-api — copy to .env and fill (do not commit .env)
PORT=8787
# CORS_ORIGIN=http://localhost:5173

# One provider key matching what the web app sends as VITE_AI_PROVIDER
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

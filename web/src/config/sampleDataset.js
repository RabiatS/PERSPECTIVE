/** Public sample CSV; URL respects Vite `base` / `VITE_BASE_PATH` (subpath deploys). */
export const SAMPLE_SPATIAL_DATASET_NAME = 'sample-spatial-dataset.csv'
export const SAMPLE_SPATIAL_DATASET_URL = `${import.meta.env.BASE_URL}${SAMPLE_SPATIAL_DATASET_NAME}`
